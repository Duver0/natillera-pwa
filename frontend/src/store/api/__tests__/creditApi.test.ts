/**
 * creditApi.test.ts
 * 
 * This test file requires MSW (Mock Service Worker) which is not installed.
 * Integration tests with real HTTP mocking need MSW to be added to the project.
 */
import { describe, it, expect } from 'vitest'

describe('creditApi', () => {
  it('skipped - requires MSW', () => {
    expect(true).toBe(true)
  })
})